fastapi>=0.100.0,<0.112.0 # Assuming a recent version compatible with Pydantic V2 features like .model_dump()
uvicorn[standard]>=0.22.0,<0.28.0
sqlalchemy>=2.0.0,<2.1.0 # Assuming SQLAlchemy 2.0 syntax if you're using it
pydantic>=2.0.0,<3.0.0 # Your .model_dump() implies Pydantic V2
# pydantic-settings>=2.0.0,<3.0.0 # Add if your config.py uses Pydantic's BaseSettings

jinja2>=3.1.0,<3.2.0
python-multipart>=0.0.5,<0.0.8 # For Form() and UploadFile
passlib[bcrypt]>=1.7.4,<2.0.0
python-jose[cryptography]>=3.3.0,<4.0.0 # For JWTs, if session_token is a JWT

# Optional, but highly recommended for DB migrations:
alembic>=1.9.0,<1.14.0

# Optional, for .env file management:
python-dotenv>=0.21.0,<1.1.0

# Add your specific database driver IF NOT using SQLite for production:
# psycopg2-binary>=2.9.5,<2.10.0 # For PostgreSQL
# mysqlclient>=2.1.1,<2.3.0    # For MySQL